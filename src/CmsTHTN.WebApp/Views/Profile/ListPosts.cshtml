﻿@{
    ViewData["Title"] = "Post list";
    ViewData["Description"] = "Post list";

}
@model ListPostByUserViewModel
<!-- Page Header-->
<header class="masthead" style="background-image: url('/assets/img/home-bg.jpg')">
    <div class="container position-relative px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-8 col-xl-7">
                <div class="site-heading">
                    <h1>Danh sách bài viết</h1>
                </div>
            </div>
        </div>
    </div>
</header>
<!-- Main Content-->
<div class="container px-4 px-lg-5">
    <div class="row gx-4 gx-lg-5 justify-content-center">
        <div class="col-md-12 col-lg-12 col-xl-12">
            <table class="table table-borderless">
                <tr>
                    <th>Tổng số</th>
                    <th>Nháp</th>
                    <th>Chờ duyệt</th>
                    <th>Đã đăng</th>
                    <th>Chờ thanh toán</th>
                    <th>Đã nhận</th>
                </tr>
                <tr>
                    <td>@Model.TotalPosts</td>
                    <td>@Model.TotalDraftPosts</td>
                    <td>@Model.TotalWaitingApprovalPosts</td>
                    <td>@Model.TotalPublishedPosts</td>
                    <td>@Model.TotalUnpaidPosts</td>
                    <td>@Model.TotalPaidAmount</td>
                </tr>
            </table>
        </div>

    </div>
    <div class="row gx-4 gx-lg-5 justify-content-center">
        <div class="col-md-12 col-lg-12 col-xl-12">
            <table class="table table-borderless">
                <thead>
                    <tr>
                        <th>Tiêu đề</th>
                        <th>Danh mục</th>
                        <th>Thời gian tạo</th>
                        <th>Lượt xem</th>
                        <th>Trạng thái</th>
                        <th></th>
                    </tr>


                </thead>
                <tbody>
                    @foreach (var item in Model.Posts.Results)
                    {
                        <tr>
                            <td><a href="@UrlConsts.EditPost/@item.Id">@item.Name</a></td>
                            <td>@item.CategoryName</td>
                            <td>@item.DateCreated</td>
                            <td>@item.ViewCount</td>
                            <td>@item.Status</td>
                            <td>
                                <a class="btn btn-success" href="@UrlConsts.SendApprovalPost?id=@item.Id">Gửi để duyệt</a>
                            </td>
                        </tr>
                    }

                </tbody>

            </table>
            @(await Component.InvokeAsync("Pager", Model.Posts))
        </div>
    </div>
</div>