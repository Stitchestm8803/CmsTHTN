@using CmsTHTN.Core.Domain.Identity
@using CmsTHTN.WebApp.Extensions
@using Microsoft.AspNetCore.Identity
@inject IOptions<SystemConfig> _config;
@model List<NavigationItemViewModel>;
@inject SignInManager<AppUser> SignInManager;
<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="/">@_config.Value.AppName</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto py-4 py-lg-0">
                <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="@UrlConsts.Home">Trang chủ</a></li>
                <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="@UrlConsts.Series">Loạt bài</a></li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle px-lg-3 py-3 py-lg-4" href="@UrlConsts.Posts" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Tin tức
                    </a>
                    <ul class="dropdown-menu">
                        @foreach (var item in Model)
                        {
                            @if (item.IsActive)
                            {
                                <li><a class="dropdown-item" title="@item.Name" href="@string.Format(UrlConsts.PostsByCategorySlug,item.Slug)">@item.Name</a></li>
                            }
                        }
                    </ul>
                </li>
                <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="@UrlConsts.Contact">Liên hệ</a></li>
            </ul>
            <form class="d-flex" role="search" asp-controller="Search" asp-action="Index" method="get">
                <input class="form-control me-2" type="search" name="keyword" placeholder="Bạn muốn tìm.." aria-label="Search">
                <button class="btn btn-outline-success" type="submit">
                    <img src="https://cdn1.iconfinder.com/data/icons/ui-set-6/100/Search-256.png" alt="Tìm" width="25" height="25" style="filter: invert(100%);">
                </button>
            </form>
            <span class="navbar-text">
                @if (SignInManager.IsSignedIn(User))
                {
                    <ul class="navbar-nav ms-auto py-4 py-lg-0">
                        <li class="nav-item"><a style="color: lightgoldenrodyellow;" class="nav-link px-lg-3 py-3 py-lg-4" href="@UrlConsts.Profile">@User.GetFirstName()</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle px-lg-3 py-3 py-lg-4" href="@UrlConsts.Posts" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Nghiệp vụ
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" title="CreatePost" href="@UrlConsts.CreatePost">Tạo bài viết</a></li>
                                <li><a class="dropdown-item" title="CreatePost" href="@UrlConsts.PostListByUser">Sửa bài viết</a></li>
                            </ul>
                        </li>
                    </ul>
                }
                else
                {
                    <a class="btn" href="@UrlConsts.Login">Đăng nhập</a>
                    <a class="btn" href="@UrlConsts.Register">Đăng ký</a>
                }
            </span>
        </div>
    </div>
</nav>